# 🚧 Active Projects

> **Purpose:** Tracks the live status of every phase, open tasks, blockers,
> and the exact next action to take. Update this file at the end of every session.
>
> **Status Key:**
> 🔴 Not Started · 🟡 In Progress · 🟢 Complete · ⛔ Blocked · ⏸️ Paused

---

## PROJECT: Credit Risk Scoring System

**Overall Progress:** `Phase 0 of 5` — Pre-development / Setup

```
Phase 1: Data & EDA          [🔴··········] 0%
Phase 2: Modelling & SHAP    [🔴··········] 0%
Phase 3: API Development     [🔴··········] 0%
Phase 4: AWS Deployment      [🔴··········] 0%
Phase 5: Docs & Presentation [🔴··········] 0%
```

---

## 📌 Phase 0 — Environment Setup
**Status:** 🟡 In Progress
**Goal:** Get local dev environment ready before writing a single line of ML code

### Tasks
- [ ] Create GitHub repository (`credit-risk-api`)
- [x] Set up project folder structure (see `preferences.md` for layout)
- [x] Create and activate Python virtual environment (`venv` or `conda`)
- [x] Install base dependencies and generate `requirements.txt`
- [x] Set up `.env` file with placeholder secrets
- [x] Generate Nigerian-context synthetic dataset (`synthetic_ng_credit_data.csv`)
- [ ] Configure VS Code / Antigravity IDE with Python linting (flake8 / pylint)
- [x] Create initial `docker-compose.yml` with PostgreSQL + Redis services

### Next Action
> 🎯 **Create the GitHub repository (`credit-risk-api`) and set up IDE.**

### Blockers
> None currently.

---

## 📌 Phase 1 — Data & Exploratory Data Analysis
**Status:** 🔴 Not Started
**Goal:** Clean, understand, and engineer features from the raw datasets

### Tasks
- [ ] Load `synthetic_ng_credit_data.csv`
- [ ] Run initial EDA (shape, dtypes, null counts, class distribution)
- [ ] Identify and document key features for the model
- [ ] Handle missing values (imputation strategy per feature type)
- [ ] Treat outliers (IQR / winsorisation)
- [ ] Engineer new features (e.g., debt-to-income ratio based on `loan_amount` and `annual_income`)
- [ ] Encode categorical features (`pd.get_dummies` or `OrdinalEncoder`)
- [ ] Analyse class imbalance (expect ~8% default rate)
- [ ] Apply SMOTE to training set
- [ ] Save cleaned dataset to `data/processed/`
- [ ] Write up EDA findings in Jupyter notebook

### Next Action
> 🎯 **After Phase 0 — download datasets and begin EDA notebook.**

### Blockers
> Waiting on Phase 0 completion.

---

## 📌 Phase 2 — Modelling & SHAP Integration
**Status:** 🔴 Not Started
**Goal:** Train 4 models, select the best, integrate SHAP explanations

### Tasks
- [ ] Implement `ModelTrainer` class (see `preferences.md`)
- [ ] Train baseline: Logistic Regression
- [ ] Train: Random Forest
- [ ] Train: XGBoost
- [ ] Train: LightGBM
- [ ] 5-fold cross-validation for all models (AUC-ROC scoring)
- [ ] Hyperparameter tuning with `GridSearchCV` on top 2 models
- [ ] Generate evaluation report:
  - [ ] AUC-ROC curves
  - [ ] Precision-Recall curves
  - [ ] Confusion matrix
  - [ ] F1, Precision, Recall table
- [ ] Select final model (expected: LightGBM or XGBoost)
- [ ] Integrate SHAP `TreeExplainer` on final model
- [ ] Build `SHAPExplainer` class with `explain(input_df)` method
- [ ] Serialise model + preprocessor to `models/model.pkl`, `models/preprocessor.pkl`

### Next Action
> 🎯 **After Phase 1 — start with Logistic Regression as the baseline.**

### Blockers
> Waiting on Phase 1 completion.

---

## 📌 Phase 3 — FastAPI Backend Development
**Status:** 🔴 Not Started
**Goal:** Build a secure, documented, production-ready REST API

### Tasks
- [ ] Scaffold FastAPI app (`app/main.py`, routers, services, schemas)
- [ ] Build `BorrowerProfile` Pydantic schema with full validation
- [ ] Build `RiskResponse` Pydantic schema
- [ ] Implement `PredictorService` class
- [ ] Implement `ScoringRepository` class (DB writes)
- [ ] JWT authentication (`/auth/token` endpoint)
- [ ] Rate limiting middleware
- [ ] Redis caching for repeated identical profiles
- [ ] `POST /score` — real-time single scoring
- [ ] `POST /score/batch` — async batch scoring via Celery
- [ ] `GET /health` — health check endpoint
- [ ] `GET /model/info` — current model version + metadata
- [ ] Set up Alembic migrations
- [ ] Full Swagger/OpenAPI docs auto-generated by FastAPI
- [ ] Build Postman collection (export as JSON)
- [ ] Write unit tests for all services

### Next Action
> 🎯 **After Phase 2 — scaffold the FastAPI structure first, then wire in the model.**

### Blockers
> Waiting on Phase 2 completion.

---

## 📌 Phase 4 — Docker & AWS Deployment
**Status:** 🔴 Not Started
**Goal:** Containerise and deploy the full system on AWS

### Tasks
- [ ] Write `Dockerfile` for the FastAPI app
- [ ] Complete `docker-compose.yml` (api + db + redis + worker)
- [ ] Test full stack locally with Docker Compose
- [ ] Create AWS account (or use existing)
- [ ] Launch EC2 instance (`t3.medium`, Ubuntu 22.04)
- [ ] Install Docker + Docker Compose on EC2
- [ ] Set up RDS PostgreSQL instance
- [ ] Create S3 bucket for model artefacts
- [ ] Upload `model.pkl` and `preprocessor.pkl` to S3
- [ ] Update app to load model from S3 using `boto3` at startup
- [ ] Configure IAM roles (EC2 → S3 access, no hardcoded keys)
- [ ] Set up VPC security groups (port 8000 open, DB port internal only)
- [ ] Run migrations on RDS
- [ ] Deploy app on EC2 via `docker-compose up -d`
- [ ] Set up CloudWatch alarms (CPU > 80%, error rate > 5%, prediction drift)
- [ ] Point a domain or Elastic IP to the EC2 instance
- [ ] Final end-to-end test on live URL

### Next Action
> 🎯 **After Phase 3 — start with Dockerfile + local Docker Compose test.**

### Blockers
> Waiting on Phase 3 completion. Will need AWS account credentials.

---

## 📌 Phase 5 — Documentation & Presentation
**Status:** 🔴 Not Started
**Goal:** Deliver all academic and technical documentation

### Tasks
- [ ] Write `README.md` for GitHub (architecture diagram, setup guide, API usage)
- [ ] Draw architecture diagram (Lucidchart / draw.io)
- [ ] Write final project report (per UNIBEN CS format)
- [ ] Prepare oral presentation slides
- [ ] Record a short API demo video (optional but impressive)
- [ ] Clean up all notebooks and add markdown commentary
- [ ] Tag GitHub repo with `v1.0.0` release

### Next Action
> 🎯 **Begin drafting the README alongside Phase 4.**

### Blockers
> Waiting on Phases 1–4 being substantially complete.

---

## 🗓️ Timeline Overview

| Phase | Target Start | Target End | Status     |
|-------|-------------|------------|------------|
| 0 — Setup        | Week 1  | Week 1  | 🟡 Active  |
| 1 — Data & EDA   | Week 2  | Week 3  | 🔴 Pending |
| 2 — Modelling    | Week 4  | Week 5  | 🔴 Pending |
| 3 — API Dev      | Week 6  | Week 8  | 🔴 Pending |
| 4 — Deployment   | Week 9  | Week 11 | 🔴 Pending |
| 5 — Docs & Pres  | Week 12 | Week 14 | 🔴 Pending |
